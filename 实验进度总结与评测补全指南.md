# TA-OPD 复现实验 · 进度总结与评测补全指南

> 基于 2026-07-25 实验产物整理。本文档衔接 `实验工作流程指导_TA-OPD.md`，
> 记录已完成的工作、现有文件清单、缺失数据，以及在新 GPU 虚拟机上补跑评测的完整步骤。

---

## A. 已完成工作总览

### A.1 环境构建（apex-llm, 6×A6000）
- [x] conda 环境 `ta_opd`（Python 3.10）
- [x] torch 2.5.1+cu124, nvcc 12.4, flash-attn 2.7.4.post1
- [x] sglang, Megatron-LM (clone+patch), apex 编译

### A.2 全流程 Pipeline（8×4090 虚拟机, 2026-07-24 ~ 07-25）

Pipeline 入口：`outputs/logs/run_all_20260724_114750.log`

| 步骤 | 内容 | 状态 |
|---|---|---|
| Step 1 | 环境搭建（conda, torch, deps） | **已完成** |
| Step 2 | 下载 Qwen3-4B + Qwen3-1.7B | **已完成** |
| Step 3 | Student HF → torch_dist 转换 | **已完成** |
| Step 4 | 准备 DAPO-Math-17k + heldout + GSM8K 数据 | **已完成** |
| Step 5 | Smoke test（2 组快速验证） | **已完成** |
| Step 6 | Main training sweep（84 次训练） | **已完成** |
| Step 7 | Diagnostic runs（heldout + gsm8k） | **已完成** |
| Step 8 | Downstream evaluation（6 基准评测） | **未完成** |
| Step 9 | 聚合结果 & 生成 Table 3 | **未完成** |

---

## B. 现有产物清单

所有产物根目录：`/inspire/hdd/project/multi-agent/zhangweinan-24046/dk/outputs/`

### B.1 Smoke Test（冒烟验证）

路径：`outputs/smoke_test/`

| 目录 | 方法 | 迭代数 | 关键文件 |
|---|---|---|---|
| `pure_opd/` | Full OPD (mask=none, ratio=1.0) | 4 (iter_0000000 ~ 003) | `latest_checkpointed_iteration.txt`, `token_bank/` |
| `ta_opd/` | TA-OPD (mask=dlearn_high, ratio=0.10) | 4 (iter_0000000 ~ 003) | 同上 |

### B.2 Main Sweep（主训练实验, 2026-07-25）

路径：`outputs/main_sweep/`

共 **~28 个 (mask, ratio, seed) 组合**，每个含 50 次 rollout 训练产物。

| 配置目录 | budget ratio | seed | 方法变体 |
|---|---|---|---|
| `k16_ratio001_seed1_20260725/` | 0.1% | 1 | dlearn_high, q3_highc |
| `k16_ratio001_seed2_20260725/` | 0.1% | 2 | dlearn_high, q3_highc |
| `k16_ratio001_seed3_20260725/` | 0.1% | 3 | dlearn_high, q3_highc |
| `k16_ratio005_seed1_20260725/` | 0.5% | 1 | dlearn_high, random, entropy, q3, q3_highc, tip, ca_softor, divergence |
| `k16_ratio005_seed3_20260725/` | 0.5% | 3 | 同上 8 种 |
| `k16_ratio010_seed3_20260725/` | 1.0% | 3 | entropy, tip, ca_softor 等 |
| `k16_ratio10_seed2_20260725/` | 10% | 2 | full |
| `k16_ratio10_seed3_20260725/` | 10% | 3 | full 等 |

**每个实验 run 目录结构**（以 `dlearn_high_max64_seed1` 为例）：

```
dlearn_high_max64_seed1/
├── iter_0000049/                    # 最终 checkpoint（torch_dist 格式）
│   ├── __0_0.distcp, __0_1.distcp   # 分片权重
│   ├── __1_0.distcp, __1_1.distcp
│   ├── .metadata, metadata.json
│   └── common.pt
├── latest_checkpointed_iteration.txt # 内容: "49"
├── token_bank/
│   ├── config.json                  # 超参（mask 方法, ratio, K=16 等）
│   ├── summary.csv                  # 50 个 rollout 的聚合统计
│   ├── merged.parquet               # 全量 token 级数据
│   └── rollout_000001.csv ~ 000050  # 每个 rollout 的逐 token 明细
├── fixed_context/
│   ├── fixed_context_metrics.parquet # 固定上下文诊断指标
│   ├── gain/
│   │   ├── regression_summary.csv    # R² 回归分析
│   │   ├── q3_bootstrap_matching_summary.csv  # Bootstrap 增益分析
│   │   └── analysis_tokens.parquet   # 逐 token 分析数据
│   └── student_hf/                  # 转换后的 HF 格式模型（用于诊断）
│       ├── config.json
│       ├── model.safetensors.index.json
│       ├── *.safetensors
│       └── tokenizer.json
└── rollout/
    └── global_dataset_state_dict_*.pt  # rollout 数据快照
```

### B.3 Diagnostics（诊断实验）

路径：`outputs/diagnostics/`

| 目录 | 训练步数 | 数据源 | 关键产物 |
|---|---|---|---|
| `gsm8k_4b_to_1p7b_300/` | 300 步 | GSM8K | checkpoint (iter 49/99/124/149/150), token_bank, fixed_context/gain/ |
| `heldout_4b_to_1p7b_300/` | 300 步 | Heldout | 同上 |
| `init_context_bank/` | 初始 | - | 初始化 context bank checkpoint |

诊断关键数据（已完成）：

**gsm8k 诊断 — regression_summary.csv**:
| model | R² | ΔR² vs H*D |
|---|---|---|
| H_D | 1.63e-04 | 0.0 (baseline) |
| H_D_C (teachability) | 1.70e-04 | +7.4e-06 |
| H_Dlearn_Dincompat | 1.35e-04 | -2.8e-05 |

Bootstrap: mean_diff=0.107, CI=[0.062, 0.171], 713 匹配对

**heldout 诊断 — regression_summary.csv**:
| model | R² | ΔR² vs H*D |
|---|---|---|
| H_D | 1.75e-05 | 0.0 |
| H_D_C | 1.75e-05 | +2.1e-08 |
| H_Dlearn_Dincompat | 1.77e-05 | +2.1e-07 |

Bootstrap: mean_diff=0.091, CI=[0.072, 0.109], 748 匹配对

### B.4 基线评测（Base Qwen3-1.7B, 训练前）

路径：`/inspire/hdd/project/multi-agent/zhangweinan-24046/dk/data/eval_results/actor_huggingface_scores.json`

| Benchmark | Avg@8 | Pass@8 |
|---|---|---|
| MATH500 | 33.75% | 75.60% |
| AMC23 | 17.50% | 52.50% |
| Minerva | 8.78% | 28.68% |
| OlympiadBench | 7.19% | 22.81% |
| AIME24 | 2.92% | 13.33% |
| AIME25 | 0.83% | 6.67% |
| **MEAN** | **11.83%** | **33.26%** |

> 注：此基线使用的是 EOPD 的 6 数学基准，与 TA-OPD 论文要求的 6 基准
> （AIME24/25, GPQA-D, HumanEval, IFEval, MATH-500）不完全一致。

### B.5 Pipeline 日志

路径：`outputs/logs/run_all_20260724_1147*.log`（约 10 个并行日志文件）

### B.6 模型权重与数据

路径：`/inspire/hdd/project/multi-agent/zhangweinan-24046/dk/`

| 资源 | 路径 |
|---|---|
| Qwen3-4B (teacher) | `modelweights/Qwen3-4B` |
| Qwen3-1.7B-Base (student) | `modelweights/Qwen3-1.7B-Base` |
| Qwen3-1.7B-Base_torch_dist | `modelweights/Qwen3-1.7B-Base_torch_dist` |
| DAPO 训练数据 | `data/dapo_train.parquet` |
| smoke 数据 | `data/smoke/smoke_train.parquet` |
| HF 缓存 | `data/hf_cache/` |

---

## C. 缺失数据（与论文 Table 3/4 对比）

### C.1 论文 Table 3 要求的 vs 现有的

论文 Table 3 要求（Qwen3-4B → Qwen3-1.7B 组, 10% budget）：

| Method | Avg. | AIME24 | AIME25 | GPQA-D. | HumanEval | IFEval | MATH-500 | 状态 |
|---|---|---|---|---|---|---|---|---|
| Base | 40.77 | 10.00 | 10.00 | 31.56 | 60.97 | 59.52 | 72.60 | **缺评测** |
| Pure OPD | 42.37 | 11.67 | 15.00 | 30.55 | 59.15 | 62.85 | 75.00 | **缺评测** |
| Entropy | 41.46 | 13.34 | 8.33 | 32.58 | 59.45 | 60.63 | 74.40 | **缺评测** |
| TIP | 43.05 | 18.34 | 11.67 | 29.29 | 62.50 | 60.91 | 75.60 | **缺评测** |
| TA-OPD | 44.89 | 20.00 | 15.00 | 34.59 | 60.98 | 64.05 | 74.70 | **缺评测** |
| TA-OPD+Ent. | 42.32 | 15.00 | 8.34 | 32.58 | 61.59 | 61.73 | 74.70 | **缺评测** |

**核心缺失：所有方法的下游 benchmark 准确率均未评测。**

### C.2 缺失项汇总

| # | 缺失项 | 说明 | 需要 GPU |
|---|---|---|---|
| 1 | **torch_dist → HF 权重转换** | 训练产物需转为 HF 格式才能被 lm-eval 加载 | 是（单卡即可） |
| 2 | **6 基准评测** | AIME24/25, GPQA-Diamond, HumanEval, IFEval, MATH-500 | 是（推理需要） |
| 3 | **5 eval seed** | 每模型×每基准跑 5 个 seed，报 mean±std | 是 |
| 4 | **Table 3 格式聚合** | 汇总为 mean±std 表格 | 否（纯 CPU 脚本） |

### C.3 Budget 档位对照

| 论文要求 | 实际训练 | 匹配情况 |
|---|---|---|
| 5% (0.05) | 0.5% (0.005) | 不匹配，差 10 倍 |
| 10% (0.10) | 10% (0.10) | **匹配** |
| 30% (0.30) | — | 未跑 |
| 50% (0.50) | — | 未跑 |

> 现有 0.1% 和 1% 的训练可做探索性参考，但 Table 4 的正式复现需补跑 5%/30%/50%。

---

## D. 新 GPU 虚拟机评测补全步骤

### D.1 机器要求

- **GPU**: 1× NVIDIA GPU (>=24GB VRAM)，推荐 4090/A6000
- **磁盘**: >=50GB 可用（模型权重 ~7GB + 评测环境 ~20GB + 产物 ~10GB）
- **CUDA**: 12.4
- **网络**: 需能访问 hf-mirror.com（下载数据集）

### D.2 环境搭建

```bash
# 1) 创建 conda 环境
conda create -n lmeval python=3.10 -y
conda activate lmeval

# 2) 安装 PyTorch (cu124)
pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124

# 3) 安装 lm-eval 及依赖
pip install lm-eval[all]
pip install transformers>=4.51.0 accelerate datasets

# 4) 验证
python -c "import lm_eval; print(lm_eval.__version__)"
python -c "import torch; print(torch.cuda.is_available(), torch.__version__)"
```

### D.3 路径配置

```bash
# 【根据新虚拟机实际情况修改以下路径】
CODE_ROOT=/path/to/taopd-baseline                    # 代码根目录（git clone 或 scp）
TAOPD_DIR=${CODE_ROOT}/TA-OPD
SLIME_DIR=${TAOPD_DIR}/slime_ta_opd

# 共享存储上的已有产物（如果新 VM 能挂载 /inspire/...）
EXISTING_ROOT=/inspire/hdd/project/multi-agent/zhangweinan-24046/dk
OUTPUT_ROOT=${EXISTING_ROOT}/outputs
MODELS=${EXISTING_ROOT}/modelweights
EVAL_ROOT=${OUTPUT_ROOT}/downstream_eval

# 如果不能挂载共享存储，需要 scp 以下文件到新 VM：
#   - modelweights/Qwen3-1.7B-Base/          （原始 student HF，~3.5GB）
#   - outputs/main_sweep/*/iter_0000049/      （训练产物 torch_dist，每个 ~7GB）
#   - outputs/diagnostics/*/iter_0000150/     （诊断训练产物）
```

### D.4 Step 1: torch_dist → HF 权重转换

```bash
#!/usr/bin/env bash
set -eo pipefail
conda activate ta_opd  # 或包含 slime_ta_opd 依赖的环境

CODE_ROOT=/path/to/taopd-baseline
SLIME_DIR=${CODE_ROOT}/TA-OPD/slime_ta_opd
MEGATRON_LM_DIR=${CODE_ROOT}/Megatron-LM
EXISTING_ROOT=/inspire/hdd/project/multi-agent/zhangweinan-24046/dk
MODELS=${EXISTING_ROOT}/modelweights
OUTPUT_ROOT=${EXISTING_ROOT}/outputs
STUDENT_HF=${MODELS}/Qwen3-1.7B-Base

export PYTHONPATH="${SLIME_DIR}:${MEGATRON_LM_DIR}:${PYTHONPATH}"

convert_one() {
  local ckpt_dir="$1"   # 含 iter_XXXXX 和 latest_checkpointed_iteration.txt 的目录
  local out_dir="$2"    # 输出 HF 目录
  if [ -f "${out_dir}/config.json" ]; then
    echo "[skip] 已转换: ${out_dir}"
    return
  fi
  echo "[convert] ${ckpt_dir} -> ${out_dir}"
  cd "${SLIME_DIR}"
  python tools/convert_torch_dist_to_hf.py \
    --input-dir "${ckpt_dir}" \
    --output-dir "${out_dir}" \
    --model-name qwen3 \
    --origin-hf-dir "${STUDENT_HF}" -f
  echo "[OK] ${out_dir}"
}

# ---- 需要转换的 checkpoint 列表 ----

# (1) Main sweep: 选 10% budget 的关键方法（对应论文 Table 3 的 10% 行）
# Pure OPD (full mask at 10%)
convert_one "${OUTPUT_ROOT}/main_sweep/k16_ratio10_seed2_20260725/full_max64_seed2" \
            "${OUTPUT_ROOT}/main_sweep/k16_ratio10_seed2_20260725/full_max64_seed2/student_hf"

# TA-OPD (dlearn_high) — 需要补跑 10% 的 dlearn_high，或从已有结果中选
# 注意：现有 10% 只有 full，dlearn_high 在 0.5% 和 0.1% 里
# 如果只需对比 pure_opd vs ta_opd，可用 0.5% 的 dlearn_high 做参考

# (2) Diagnostics: 300 步训练产物
convert_one "${OUTPUT_ROOT}/diagnostics/gsm8k_4b_to_1p7b_300" \
            "${OUTPUT_ROOT}/diagnostics/gsm8k_4b_to_1p7b_300/student_hf"
convert_one "${OUTPUT_ROOT}/diagnostics/heldout_4b_to_1p7b_300" \
            "${OUTPUT_ROOT}/diagnostics/heldout_4b_to_1p7b_300/student_hf"

echo "===== 权重转换完成 ====="
```

### D.5 Step 2: 下游 6 基准评测

```bash
#!/usr/bin/env bash
set -eo pipefail

# ---------- 路径配置 ----------
EXISTING_ROOT=/inspire/hdd/project/multi-agent/zhangweinan-24046/dk
MODELS=${EXISTING_ROOT}/modelweights
OUTPUT_ROOT=${EXISTING_ROOT}/outputs
STUDENT_HF=${MODELS}/Qwen3-1.7B-Base
EVAL_ROOT=${OUTPUT_ROOT}/downstream_eval

CONDA_ENV=lmeval
SEEDS=(0 1 2 3 4)

export HF_ENDPOINT="${HF_ENDPOINT:-https://hf-mirror.com}"
export HF_HOME="${HF_HOME:-${EXISTING_ROOT}/data/hf_cache}"
export HF_HUB_CACHE="${HF_HUB_CACHE:-${HF_HOME}/hub}"
export HF_DATASETS_CACHE="${HF_DATASETS_CACHE:-${HF_HOME}/datasets}"

source "$(conda info --base)/etc/profile.d/conda.sh"
conda activate "${CONDA_ENV}"
mkdir -p "${EVAL_ROOT}"

# ---------- 模型列表 ----------
# 格式: "名称|GPU编号|HF模型路径"
# 根据实际转换结果调整
MODELS=(
  "base|0|${STUDENT_HF}"
  "pure_opd|0|${OUTPUT_ROOT}/main_sweep/k16_ratio10_seed2_20260725/full_max64_seed2/student_hf"
  "ta_opd|0|${OUTPUT_ROOT}/main_sweep/k16_ratio005_seed1_20260725/dlearn_high_max64_seed1/fixed_context/student_hf"
)

# ---------- 6 基准（TA-OPD 论文 Table 3 口径） ----------
# 注意：task 名需与你安装的 lm-eval 版本匹配，先用 --tasks list 查看可用任务
TASKS="aime24,aime25,gpqa_diamond,humaneval,ifeval,math_500"

# ---------- 评测函数 ----------
run_model() {
  local name="$1" gpu="$2" model="$3"
  local model_dir="${EVAL_ROOT}/${name}"
  mkdir -p "${model_dir}"

  for SEED in "${SEEDS[@]}"; do
    local seed_dir="${model_dir}/seed${SEED}"
    local done_flag="${seed_dir}/DONE"
    [ -f "${done_flag}" ] && { echo "[skip] ${name} seed${SEED}"; continue; }
    mkdir -p "${seed_dir}"

    echo "[$(date '+%F %T')] START ${name} seed=${SEED} gpu=${gpu}"
    set +e
    CUDA_VISIBLE_DEVICES="${gpu}" timeout 600m lm-eval run \
      --model hf \
      --model_args "pretrained=${model},dtype=bfloat16,trust_remote_code=True" \
      --tasks "${TASKS}" \
      --device cuda:0 \
      --batch_size auto \
      --seed "${SEED}" \
      --output_path "${seed_dir}" 2>&1 | tee -a "${model_dir}/eval.log"
    local ec=${PIPESTATUS[0]}
    set -e

    if [ "${ec}" -eq 0 ]; then
      date '+%F %T' > "${done_flag}"
      echo "[OK] ${name} seed${SEED}"
    else
      echo "[FAIL] ${name} seed${SEED} exit=${ec}"
    fi
  done
}

# ---------- 执行评测 ----------
echo "===== 开始下游评测 ====="
for entry in "${MODELS[@]}"; do
  IFS='|' read -r mname mgpu mpath <<< "${entry}"
  if [ ! -d "${mpath}" ]; then
    echo "[WARN] 模型目录不存在，跳过: ${mpath}"
    continue
  fi
  run_model "${mname}" "${mgpu}" "${mpath}"
done
echo "===== 所有模型评测完成 ====="
```

### D.6 Step 3: 聚合结果 → Table 3 格式

```bash
#!/usr/bin/env bash
# 评测完成后运行此脚本，聚合 mean±std 并输出 Table 3 风格表格
set -eo pipefail

EXISTING_ROOT=/inspire/hdd/project/multi-agent/zhangweinan-24046/dk
EVAL_ROOT=${EXISTING_ROOT}/outputs/downstream_eval
SEEDS=(0 1 2 3 4)

python3 - "${EVAL_ROOT}" "${SEEDS[*]}" <<'PYEOF'
import json, csv, glob, statistics, pathlib, sys

EVAL_ROOT = sys.argv[1]
SEEDS = [int(s) for s in sys.argv[2].split()]

BENCH_DISP = ["AIME24", "AIME25", "GPQA-D", "HumanEval", "IFEval", "MATH-500"]
BENCH_TASK = ["aime24", "aime25", "gpqa_diamond", "humaneval", "ifeval", "math_500"]
MODELS = ["base", "pure_opd", "ta_opd"]

# 每个 task 的候选指标键（按优先级取第一个存在的）
CAND = {
    "aime24":       ["exact_match"],
    "aime25":       ["exact_match"],
    "gpqa_diamond": ["acc_norm", "exact_match", "acc"],
    "humaneval":    ["pass@1", "humaneval_pass@1", "pass_at_1"],
    "ifeval":       ["prompt_level_strict_acc", "inst_level_strict_acc", "exact_match"],
    "math_500":     ["exact_match"],
}

def grab(task, res):
    for k in CAND.get(task, []):
        if k in res:
            return res[k]
    return f"N/A(keys={list(res.keys())})"

rows = {}
for m in MODELS:
    rows[m] = {t: [] for t in BENCH_TASK}
    for s in SEEDS:
        files = sorted(pathlib.Path(EVAL_ROOT) / m / f"seed{s}" / "results_*.json")
        if not files:
            print(f"[WARN] {m} seed{s}: 无 results_*.json")
            continue
        data = json.loads(files[-1].read_text())
        results = data.get("results", {})
        for t in BENCH_TASK:
            v = grab(t, results.get(t, {}))
            try:
                rows[m][t].append(float(v))
            except (ValueError, TypeError):
                rows[m][t].append(None)

def meanstd(vals):
    vs = [v for v in vals if v is not None]
    if not vs:
        return ("", "")
    mn = statistics.mean(vs)
    sd = statistics.pstdev(vs) if len(vs) > 1 else 0.0
    return (f"{mn:.2f}", f"{sd:.2f}")

lines = [
    "# TA-OPD 下游评测摘要（mean±std, 5 eval seeds）",
    "",
    "| Model | Avg | AIME24 | AIME25 | GPQA-D | HumanEval | IFEval | MATH-500 |",
    "| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |",
]
csv_rows = [["model", "avg"] + BENCH_DISP]

for m in MODELS:
    cells = [m]
    avg_vals = []
    for t in BENCH_TASK:
        mn, sd = meanstd(rows[m][t])
        cells.append(f"{mn}±{sd}" if mn else "n/a")
        if mn:
            avg_vals.append(float(mn))
    avg = f"{statistics.mean(avg_vals):.2f}" if avg_vals else ""
    lines.append("| " + " | ".join([m, avg] + cells[1:]) + " |")
    csv_rows.append([m, avg] + [c for c in cells[1:]])

print("\n".join(lines))

out_md = pathlib.Path(EVAL_ROOT) / "summary_table3.md"
out_md.write_text("\n".join(lines) + "\n")
with open(pathlib.Path(EVAL_ROOT) / "summary_table3.csv", "w", newline="") as f:
    w = csv.writer(f)
    w.writerow(csv_rows[0])
    w.writerows(csv_rows[1:])

print(f"\n[OK] 已写出 {out_md} 与 summary_table3.csv")
PYEOF
```

### D.7 快速检查清单

在新 VM 上执行前，逐项确认：

- [ ] `conda activate lmeval` 可用，`lm-eval --version` 正常
- [ ] `python -c "import torch; print(torch.cuda.is_available())"` 返回 True
- [ ] 能访问 `/inspire/hdd/project/multi-agent/zhangweinan-24046/dk/` 共享存储
  - 或已将模型权重 scp 到本地
- [ ] `modelweights/Qwen3-1.7B-Base/` 存在且含 `config.json`
- [ ] 训练产物 `main_sweep/*/iter_0000049/` 存在且含 `latest_checkpointed_iteration.txt`
- [ ] HF 数据集缓存目录可写（评测需下载 AIME/GPQA 等数据集）
- [ ] 磁盘剩余 >= 50GB

---

## E. 与论文对比的关键注意点

1. **Budget 档位**：论文 Table 4 要求 5%/10%/30%/50%，现有训练只有 0.1%/0.5%/1%/10%。
   如需完整复现 Table 4，需在训练机上补跑 5%/30%/50% 三档。

2. **Teacher-Student 组合**：论文 Table 3 有 4 组（4B→1.7B, 8B→4B, 14B→4B, R1→3B），
   现有仅完成第 1 组（Qwen3-4B → Qwen3-1.7B）。其余 3 组需额外训练+评测。

3. **评测基准差异**：现有基线用的是 EOPD 的 6 数学基准（MATH500/AMC23/Minerva/
   OlympiadBench/AIME24/25）。TA-OPD 论文要求的是 AIME24/25/GPQA-D/HumanEval/
   IFEval/MATH-500，含代码和指令遵循基准，**不能混用**。

4. **lm-eval task 名称**：不同版本 lm-eval 的 task 名可能不同。
   评测前先用 `lm-eval --tasks list` 确认 `aime24`, `gpqa_diamond`, `humaneval`,
   `ifeval`, `math_500` 等 task 名是否可用，不可用则需查找对应替代名或手动注册。

5. **5 eval seed**：论文要求每 checkpoint 用 5 个 seed 评测报 mean±std，
   不是跑 1 次取单值。这是误差棒的关键。

---

## F. 预估资源与时间

| 步骤 | GPU 需求 | 预估时间 |
|---|---|---|
| 环境搭建（D.2） | 不需要 | ~30 分钟 |
| 权重转换（D.4） | 1× GPU, >=24GB | 每 checkpoint ~5-10 分钟 |
| 评测（D.5） | 1× GPU, >=24GB | 每模型×每seed ~1-3 小时 |
| 聚合（D.6） | 不需要 | < 1 分钟 |

**最小方案**（仅对比 Base / Pure OPD / TA-OPD）：
- 3 模型 × 5 seed = 15 次评测
- 单卡 4090 预估 **1-2 天**

**完整方案**（论文 Table 3 全部 6 方法 × 5 seed）：
- 需先补跑缺失方法的权重转换
- 6 模型 × 5 seed = 30 次评测
- 单卡 4090 预估 **2-4 天**

---

## G. 2026-07-29 评测执行记录（本机 8×4090）

实际执行与本文原假设的关键偏差（必读，避免误判进度）：

1. **权重转换（torch_dist→HF）此前其实已完成**。经核对，`main_sweep` 下多数 run 已有
   `fixed_context/student_hf`（含 10% 的 `full`、1% 的 `entropy/tip/dlearn_high/ca_softor`），
   可直接被 lm-eval 加载。原 D.4「待转换」已过时，无需再转。
2. **本机即 8×4090 虚拟机**（非「新 VM」），可直接挂载 `/inspire/...` 共享存储，
   模型权重与训练产物均在本地，无需 scp。
3. **GPQA-Diamond 为 gated 数据集**，当前环境无 `HF_TOKEN`，故只评 5 个可下载基准
   （aime24 / aime25 / humaneval / ifeval / hendrycks_math500）；GPQA 待 token 就绪后单独补跑再聚合。

### G.1 环境构建（conda env=lmeval）
- torch 2.5.1+cu124 + lm-eval 0.4.12 + transformers 5.x
- **踩坑修复（重跑必遇，已解决）**：
  - `lm-eval[all]` **未自动安装** ifeval 依赖 `langdetect` / `immutabledict`，导致 `ModuleNotFoundError`；
    需额外 `pip install langdetect immutabledict math-verify`。
  - aime24/aime25 的 YAML `max_gen_toks: 32768` 等于 Qwen3-1.7B 的 `max_position_embeddings`(32768)，
    触发 `assert max_gen_toks < max_seq_len`；已将
    `site-packages/lm_eval/tasks/aime/aime24.yaml` 与 `aime25.yaml` 的 `max_gen_toks` 改为 **16384**
    （aime 解答远短于此，足够）。
  - `humaneval` 标记为 unsafe，需加 `--confirm_run_unsafe_code`。
  - 本机默认 shell 为 `sh`，`source`/`conda activate` 不可用；评测脚本须用 `bash` 执行。

### G.2 评测脚本与模型清单
脚本：`指令_下游评测_ta_opd.txt`（已更新为 6 方法 × 5 seed，8 卡并行）。
聚合脚本：`aggregate_table3.py`（纯 stdlib，读 `downstream_eval/<model>/seed<X>/results_*.json`，
输出 `summary_table3.md` / `summary_table3.csv`，5 eval seed 的 mean±std）。

| method | HF 路径（相对 outputs/main_sweep） | budget |
|---|---|---|
| base | `modelweights/Qwen3-1.7B-Base` | base |
| pure_opd (full) | `k16_ratio10_seed2/full_max64_seed2/student_hf` | 10% |
| ta_opd (dlearn_high) | `k16_ratio005_seed1/dlearn_high_max64_seed1/fixed_context/student_hf` | 0.5% |
| entropy | `k16_ratio010_seed2/entropy_max64_seed2/fixed_context/student_hf` | 1% |
| tip | `k16_ratio010_seed2/tip_max64_seed2/fixed_context/student_hf` | 1% |
| ta_opd_entropy (ca_softor) | `k16_ratio010_seed2/ca_softor_max64_seed2/fixed_context/student_hf` | 1% |

> ⚠️ 论文 Table 3 要求 6 方法**全部 10%**，但本地训练仅 `pure_opd(full)` 有 10% checkpoint，
> 其余方法最高只到 1% / 0.5%。上表按各方法「可用最高 budget」评测，聚合表会标注 budget，
> **不可与论文同 budget 直接横比**。要完整复现论文 10% 行，需在训练机补跑 5 方法 @10%（~300 轮）。

### G.3 状态（截至脚本启动）
- Step 8 下游评测：**进行中**（8 卡并行，30 次评测；单模型 ~1–2.5h，预计总墙钟 ~8–15h）
- Step 9 聚合：待 Step 8 完成后执行 `python3 aggregate_table3.py`
- 进度监控：`/tmp/watch_eval.sh`（轮询 DONE 计数，30/30 即全部完成）

### G.4 首轮失败与修复（2026-07-30 凌晨，重要踩坑）
首轮 30 次评测**全部失败（ec=1）**，两处根因：
1. **NLTK `punkt_tab` 资源缺失**：lm-eval 在指标阶段调用 `nltk.PunktTokenizer`，
   报 `Resource 'punkt_tab' not found`。`lm-eval[all]` 不随附该数据，且 `nltk.downloader`
   从 nltk.org 下载在此网络极慢/卡死。已改从 github raw 直接拉取并解压：
   `curl -L .../nltk/nltk_data/gh-pages/packages/tokenizers/punkt_tab.zip`
   → 解压到 `/root/nltk_data/tokenizers/punkt_tab/`（同时装 `punkt`）。
2. **多实例共享 GPU 触发 CUDA OOM**：脚本 `gpu=gi%8` 使每卡最多 ~4 个实例并发，
   aime 的 `max_gen_toks=16384` 单实例峰值 ~40GB，交接瞬间两实例叠加 > 47GB → OOM。
   已将 aime24/aime25 YAML 的 `max_gen_toks` 由 16384 改为 **4096**（1.7B 解答远短于此，足够），
   并在脚本加 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 减少碎片。

修复后措施：
- 杀掉首轮所有残留 lm-eval 进程（注意：本机 `pkill` 在前台命令里会被 harness 中断，
  须用 **run_in_background** 的后台任务执行 kill 才能成功）。
- 重跑脚本加 `--cache_requests`：生成结果落盘缓存，后续若再出问题重跑只需重算指标、极快。
- 已重跑（task hhj2N5），watcher 重启（PKBwwy）。G.3 的时长估计对 4096 版本偏长，实际更快。

### G.4.1 二次踩坑：`--batch_size auto` 把显存填到上限
重跑后发现 8 卡同时跑到 **42–48GB（仅 ~0.8GB 余量）**。排查 run.log：
lm-eval 报 `max_length=4930`，且 `--batch_size auto` 会把同一卡上的并行序列数
填满到显存上限，KV-cache（≈ batch×56KB×4930）是显存主体。单实例 aime 阶段即逼近
48GB 上限，任何波动都会 OOM。已将 `--batch_size auto` 改为 **`--batch_size 8`**
（单实例 ~10GB，余量充足）。注意：本机实为 ~48GB 卡（非 8×4090）。
- 顺带：aime24/25 的 `max_gen_toks` 已是 4096（1.7B 解答远够），主要显存来自 KV，
  故降 gen_toks 帮助有限，关键在限 batch。
