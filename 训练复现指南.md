# TA-OPD 训练复现指南（从零 pull → 重新开始训练）

> 本文档说明：在一台新机器上 `git clone` 本仓库后，如何从零搭建环境、准备数据、跑通 smoke、
> 正式训练，并跑下游评测。所有步骤都对应仓库里的 `指令_*.txt` 与 `TA-OPD/slime_ta_opd/` 下的启动脚本。
>
> ⚠️ 当前下游评测结果**已知有问题、待重修**（0-shot 评测对 base 模型是坏的，MATH-500≈0.4% 属协议问题）。
> 评测已改为 few-shot（见 `指令_下游评测_ta_opd.txt`），重训重评后再下结论。详见根 `README.md`。

---

## 0. 最重要的前提：路径与用户名是硬编码的

仓库里的 `指令_*.txt` 顶部都写死了项目共享路径与用户名：

- `CODE_ROOT=/inspire/hdd/project/multi-agent/zhangweinan-24046/dk/CacheOPD/taopd-baseline`
- `MODELS=/inspire/.../dk/modelweights`
- `DATA_DIR=/inspire/.../dk/data`
- `OUTPUT_ROOT=.../outputs/slime_opd`
- 环境脚本里还有 `TMPDIR=/home/kejiechen/...` 和 conda 探测路径 `/home/kejiechen/miniconda3`

**换机器第一步**：把每个脚本顶部的 `CODE_ROOT / MODELS / DATA_DIR / OUTPUT_ROOT` 改成你的实际路径；
环境脚本里的 `TMPDIR` 和 conda 路径也按你的用户名/家目录改。推荐用环境变量覆盖，例如
`CODE_ROOT=/your/path bash 指令_环境准备_ta_opd.txt`。

---

## 1. 目录结构要求

`指令_环境准备_ta_opd.txt` 会把 `Megatron-LM` 克隆成 `CODE_ROOT` 的**兄弟目录**：

```
<CODE_ROOT>/                  (本仓库 clone 到这里)
├── TA-OPD/
│   └── slime_ta_opd/         # 完整训练树（train.py / 启动脚本 / docker/patch）
├── Megatron-LM/              # 由环境脚本 clone + patch，必须在此路径
└── outputs/slime_opd/        # 训练产物（已被 .gitignore 排除）
```

---

## 2. 阶段 0：搭建 `ta_opd` 训练环境

```bash
bash 指令_环境准备_ta_opd.txt
```

做了什么（幂等，失败重跑会跳过已完成步骤）：

- 创建 conda 环境 `ta_opd`（Python 3.10），锁死 **torch 2.5.1+cu124**（用 `PIP_CONSTRAINT` 防止被 requirements 拽高到 cu13）。
- 编译安装 flash-attn 2.7.4.post1、apex（`--cpp_ext --cuda_ext`）。
- clone `NVIDIA/Megatron-LM` 到固定 commit 并打 `megatron.patch`，`pip install -e . --no-deps`。
- 安装 sglang 0.5.10.post1（打 `sglang.patch` 放宽 torch 版本锁）、TransformerEngine 2.10（4090 用 `--transformer-impl local` 时不强依赖）、slime 及其三方依赖。
- 末尾会导入自检（`torch/sglang/ray/megatron.core/...`）并打印 `SLIME_DIR / MEGATRON_LM / CONDA_ENV`。

注意：原流程在 **apex-llm（6×A6000）** 上搭环境，因为 A6000 自带 nvcc 12.1 与 torch cu124 不符，
脚本提供了 `FORCE_CONDA_NVCC=1` 开关改用 conda-forge 的 cuda-nvcc 12.4。在 4090/其他卡上确保
**nvcc 12.4 ↔ torch cu124** 一致即可。

---

## 3. 阶段 1：下载模型 + 权重转换 + 准备训练数据

```bash
bash 指令_权重转换_ta_opd.txt
```

- 下载 teacher `Qwen3-4B`（SGLang 直接用 HF，无需转换）与 student `Qwen3-1.7B-Base`。
- **student HF → torch_dist**：`convert_qwen3_1_7b_to_torch_dist.sh`（Megatron 训练只吃 torch_dist 格式）。
- DAPO prompts → `dapo_train.parquet`（字段名 `prompt`）。脚本里默认
  `ds_name="diagnoseailab/dapo-math-17k"`，**请换成你实际拿到的 DAPO 数据集**。

收尾会校验四个关键产物是否存在（teacher / student HF / student torch_dist / parquet）。

---

## 4. 阶段 2：Smoke 打通 pipeline（必做的小验证）

```bash
bash 指令_smoke_4b_to_1p7b.txt
```

对两个方法各跑极小训练（`NUM_ROLLOUT=4`）：

- `pure_opd`：`OPD_BUDGET_MASK=none`（全 response token 作直接 OPD 目标）
- `ta_opd`：`OPD_BUDGET_MASK=dlearn_high`，budget 0.10

GPU 分配由脚本顶部环境变量控制（`TEACHER_GPU / RAY_GPUS / ACTOR_NUM_GPUS_PER_NODE / ROLLOUT_NUM_GPUS`），
原设 8 卡：teacher 占 1 卡，actor/rollout 占其余。

跑完检查 `outputs/slime_opd/pure_opd` 与 `outputs/slime_opd/ta_opd` 下是否生成
`latest_checkpointed_iteration.txt`——能出 ckpt 说明 环境+数据+训练链路 全通。

---

## 5. 阶段 3：正式训练（从零重训）

### (a) 方法套（推荐，复刻论文多方法对比）

```bash
cd TA-OPD/slime_ta_opd
export SLIME_DIR=$PWD
export MEGATRON_LM_DIR=<你的 Megatron-LM 路径>
export OUTPUT_ROOT=<你的 outputs/slime_opd>
export OPD_TOPK_METRICS_K=16          # 论文统一 support 统计量 K=16

# ratio = 监督 token budget；要匹配论文统一 10% 就把每个方法都设 0.10
# （pure_opd 强制 1.0）。要复现 Table 4 的 30%/50% 再加 :0.30: / :0.50: 条目
export METHOD_LIST="pure_opd:1.0:10 teachability:0.10:20 entropy:0.10:30 teachability_entropy:0.10:40 tip:0.10:50"

bash run_teachability_opd_method_suite_20260515.sh   # → run_opd_budget_ratio_sweep_20260512.sh
```

方法名→mask 映射：`pure_opd→full`、`teachability→dlearn_high`、`entropy→entropy`、
`teachability_entropy→ca_softor`、`tip→tip`、`divergence→divergence`、`random→random`。

完整 run 约 **300 轮 rollout**（smoke 只 4 轮），请把 sweep 脚本里的 `NUM_ROLLOUT` 调大。

### (b) 单方法最小启动（等价于 smoke 但放大）

直接调 smoke 用的入口，放大 rollout 数、设定目标 budget：

```bash
RUN_NAME=ta_opd_r10 SAVE_DIR=outputs/slime_opd/ta_opd \
NUM_ROLLOUT=300 ROLLOUT_BATCH_SIZE=... GLOBAL_BATCH_SIZE=... \
OPD_BUDGET_MASK=dlearn_high OPD_BUDGET_RATIO=0.10 \
OPD_TOKEN_BANK_PAIR_ID=qwen3_4b_to_qwen3_1p7b \
bash TA-OPD/slime_ta_opd/examples/on_policy_distillation/run-qwen3-1.7B-sampled-opd-sglang.sh
```

> 注：`repro_base_submit.sh` + `repro*.py` 不是训练本体，而是 8×4090 VM 上诊断
> Ray / `torch.dtype` 序列化用的辅助脚本（`bash repro_base_submit.sh repro6.py` 跑一遍自检）。
> 正式训练走上面的 (a)/(b) 即可。

---

## 6. 阶段 4：下游评测（已改 few-shot，**重跑前务必清旧产物**）

```bash
# 先跑 base 一个 seed 验证 MATH-500 从 0.4% 回升到合理区间（>10）
CUDA_VISIBLE_DEVICES=0 lm-eval --model hf \
  --model_args "pretrained=<student HF>,dtype=bfloat16,trust_remote_code=True" \
  --tasks hendrycks_math500 --num_fewshot 4 --fewshot_as_seed \
  --device cuda:0 --batch_size 8 --seed 0 --limit 10 \
  --confirm_run_unsafe_code --output_path /tmp/verify_math

# 验证通过后，全量重跑 6 方法 × 5 seed（FORCE_RERUN=1 清掉旧 0-shot 产物）
FORCE_RERUN=1 bash 指令_下游评测_ta_opd.txt

# 聚合到 summary_table3.md / .csv
python3 aggregate_table3.py
```

要点：

- 评测脚本已把数学基准改为 **few-shot**（`--num_fewshot 4`，带 `\boxed{}` 示例），few-shot 失败会自动回退 0-shot。
- **`FORCE_RERUN=1` 必须加**：旧 0-shot 的 `results_*.json` 会和新 few-shot 结果混在一起被聚合，导致数字错乱。
- 评测机器建议 1 实例/卡、开 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments`、`--batch_size 8`（详见脚本内注释）。
- GPQA-Diamond 为 gated 数据集，需 `HF_TOKEN`（且账号有 `Idavidrein/gpqa` 权限）单独补跑。

---

## 7. 迁移到新机器的 checklist

- [ ] 每个 `指令_*.txt` 顶部的 `CODE_ROOT / MODELS / DATA_DIR / OUTPUT_ROOT` 已改成新路径
- [ ] 环境脚本里的 `TMPDIR` 与 conda 路径（`/home/kejiechen/...`）已按新用户名/家目录修改
- [ ] `nvcc --version` = 12.4，且与 torch cu124 一致（否则 apex 编译/运行会 ABI 报错）
- [ ] `Megatron-LM` 已作为 `CODE_ROOT` 的兄弟目录存在（环境脚本会自动 clone+patch）
- [ ] DAPO 数据集名已在 `指令_权重转换_ta_opd.txt` 中替换为实际数据集
- [ ] 评测重跑使用 `FORCE_RERUN=1`，且先验证 MATH-500 已回升再跑全量

---

## 8. 相关文件速查

| 步骤 | 脚本 | 说明 |
|---|---|---|
| 环境 | `指令_环境准备_ta_opd.txt` | 建 `ta_opd` 环境 + 装 Megatron-LM/apex/sglang/slime |
| 数据/权重 | `指令_权重转换_ta_opd.txt` | 下模型、HF→torch_dist、DAPO→parquet |
| Smoke | `指令_smoke_4b_to_1p7b.txt` | 小训练验证链路 |
| 训练入口 | `TA-OPD/slime_ta_opd/examples/on_policy_distillation/run-qwen3-1.7B-sampled-opd-sglang.sh` | 单跑训练 |
| 方法套 | `TA-OPD/slime_ta_opd/run_teachability_opd_method_suite_20260515.sh` | 多方法 sweep 入口 |
| 评测 | `指令_下游评测_ta_opd.txt` | 6 基准下游评测（已改 few-shot） |
| 聚合 | `aggregate_table3.py` | 聚合 → `summary_table3.md/.csv` |
